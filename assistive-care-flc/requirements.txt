# ══════════════════════════════════════════════════════════════════════
# Pinned environment for the Task 2 fuzzy + evolutionary work.
#
# Verified working on Python 3.13.13 / Windows, 2026-09-06.
#
#   python -m venv acflc-venv
#   acflc-venv\Scripts\activate          # Windows
#   pip install -r requirements.txt
#   pytest                               # 12 tests, 4 need scikit-fuzzy
# ══════════════════════════════════════════════════════════════════════

# ── Runtime: everything Parts 1-3 need ────────────────────────────────
numpy==2.5.1
scipy==1.18.0
matplotlib==3.11.1

# ── Test-only: the independent oracle ─────────────────────────────────
# scikit-fuzzy is NOT a runtime dependency and is deliberately not
# imported by anything under src/. The Mamdani engine in
# acflc/controller.py is hand-written; scikit-fuzzy exists here solely to
# cross-check it in tests/test_against_skfuzzy.py. An oracle that shared
# code with the thing it checks would not be an oracle.
scikit-fuzzy==0.5.0
pytest==9.0.1
